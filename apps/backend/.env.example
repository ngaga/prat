# Copy to .env.local (not committed). Server-only; never use NEXT_PUBLIC_* here.

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001


# Optional (production): comma-separated browser origins for CORS. If unset, CORS reflects Origin.
# FRONTEND_ORIGIN=
