# Copy to .env.local (not committed). Client-exposed keys use NEXT_PUBLIC_* only.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Production: public URL of the Nest API (HTTPS, no trailing slash).
# Local dev: usually omit so dev.sh + NEXT_PUBLIC_NEST_PORT target the correct port.
# NEXT_PUBLIC_BACKEND_URL=

# NEXT_PUBLIC_KOFI_USERNAME=
