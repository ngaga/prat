services:
  - type: web
    name: prat
    runtime: node
    buildCommand: corepack enable && pnpm install && pnpm run build
    startCommand: pnpm run start
    envVars:
      - key: NODE_VERSION
        value: "20"
